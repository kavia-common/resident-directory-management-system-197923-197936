# Backend configuration (do not commit real secrets)

# Database: either set DATABASE_URL/POSTGRES_URL, or ensure resident_db/db_connection.txt exists
DATABASE_URL=postgresql://appuser:dbuser123@localhost:5000/myapp
# POSTGRES_URL=postgresql://appuser:dbuser123@localhost:5000/myapp

# JWT (required)
JWT_SECRET_KEY=change_me_to_a_long_random_secret
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRES_MINUTES=30
REFRESH_TOKEN_EXPIRES_DAYS=30
JWT_ISSUER=resident-directory
JWT_AUDIENCE=resident-directory-frontend

# CORS (optional, comma-separated)
ALLOW_ORIGINS=http://localhost:3000

# Cloudinary placeholders (stub only for now)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=
